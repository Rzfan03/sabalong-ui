---
title: Switch
description: Toggle between two states, on or off.
---

### Switch
<div className="h-fit border border-zinc-700 rounded-lg p-8 flex justify-center items-center">
import SwitchDemo from "@/app/components/Switch";

<SwitchDemo />
</div>

### Usage
```tsx
import { Switch } from "sabalong-ui";

<Switch label="Dark Mode" defaultChecked onChange={(on) => {}} />
```

Use `checked` instead of `defaultChecked` for a fully controlled switch.
